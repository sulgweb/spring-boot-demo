spring:
  application:
    name: baize-sb

  datasource:
    url: jdbc:mysql://127.0.0.1:3306/springboot_demo?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: admin
    password: admin123
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    redis:
      host: localhost
      port: 6379
      password: redis123
      database: 0

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    org.springframework.boot.autoconfigure.logging.ConditionEvaluationReportLogger: OFF
